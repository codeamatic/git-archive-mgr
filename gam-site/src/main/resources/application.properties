# ----------------------------------------
# SPRING CORE PROPERTIES
# ----------------------------------------

# LOGGING
# logging.level.org.springframework=DEBUG

# EMBEDDED SERVER CONFIGURATION (ServerProperties)
server.port=8888

# FREEMARKER
spring.freemarker.cache=false
spring.freemarker.expose-spring-macro-helpers=true
spring.freemarker.settings.tag_syntax=square_bracket

# DataSource settings: set here your own configurations for the database
# connection.
spring.datasource.url = jdbc:mysql://10.4.0.55:3306/gam
spring.datasource.username = rockdev
spring.datasource.password = I4mrockf
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

gam.builddirectory.base = /opt/gam/build